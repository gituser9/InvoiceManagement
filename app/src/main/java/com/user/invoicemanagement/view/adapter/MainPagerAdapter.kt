package com.user.invoicemanagement.view.adapter


/*
class MainPagerAdapter(manager: FragmentManager, private var numOfTabs: Int, var factoryMap: Map<Int, ProductFactory>): FragmentStatePagerAdapter(manager) {

    var tabPosition = 0

    override fun getItem(position: Int): Fragment {
        if (position == tabPosition) {
            val fragment = MainTabFragment()
            fragment.factory = factoryMap[position] ?: ProductFactory()

            return fragment
        } else {
            return Fragment()
        }
    }

    override fun getCount(): Int = numOfTabs
}*/
